# mclaw 桌面端环境变量样例（复制为 .env 后填入真实值）

# ─── Go 后端地址 ─────────────────────────────────────────────
# 桌面端通过 Vite dev server 代理 /api → backend，避免跨域 cookie 问题。
#
# dev 模式（pnpm dev）：留空即可，vite.config.ts 会从本文件读 VITE_API_BASE_URL
#                      作为 /api 代理目标（同源请求，后端 SameSite=Lax 的
#                      session cookie 才能在 login 后被 /users/status 正确携带）。
# prod 构建（pnpm package / 打包安装）：必须填写 backend 公网入口。
# VITE_API_BASE_URL=https://your-mclaw-backend.example.com

# ─── 后端 llmproxy 转发地址 ──────────────────────────────────
# OpenClaw Gateway 是用户本地进程，不走 Vite proxy，云端模型对话须直连后端
# llmproxy（/v1/*）转发到真实大模型 + 计费。dev/prod 都用后端公网地址。
VITE_LLMPROXY_BASE_URL=https://your-mclaw-backend.example.com/v1

# ─── OpenClaw Gateway ────────────────────────────────────────
OPENCLAW_GATEWAY_PORT=18789

# ─── 开发服务器 ───────────────────────────────────────────────
VITE_DEV_SERVER_PORT=5173

# ─── 打包发布（CI/CD）─────────────────────────────────────────
# Apple Developer Credentials
APPLE_ID=your@email.com
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_TEAM_ID=XXXXXXXXXX

# Code Signing Certificate
CSC_LINK=path/to/certificate.p12
CSC_KEY_PASSWORD=certificate_password

# GitHub Token for releases
GH_TOKEN=github_personal_access_token
